

`devenv processes list` and `devenv processes wait` talk to the running process-compose
server over its control socket, using the same package as `devenv up`.

[comment]: # (Please add your documentation on top of this line)

@AUTOGEN_OPTIONS@
